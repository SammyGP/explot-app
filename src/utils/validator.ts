import * as df from "data-forge"

/**
 * Validate the file submision before we parse it
 */

/**
 * Validate Data and Columns before we chart them
 */
export const chartDataValidator = () => {}

/**
 * Sets the datatypes of the columns in the dataframe
 * @param dataframe
 */
export const dataframeColumns = (dataframe: df.DataFrame) => {}

/**
 * Sets the @dataType for the column submitted
 */
export const chartDataColumnTypes = (dataframe: df.DataFrame) => {}

export const validateColumn = (
  columnName: string,
  dataframe: df.DataFrame
) => {}
